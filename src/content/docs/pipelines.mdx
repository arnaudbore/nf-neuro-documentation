---
title: Pipelines
description: Discover the pipelines powered by nf-neuro
---

import { getCollection } from 'astro:content';
import PipelineCard from '../../components/PipelineCard.astro';

export const pipelines = await getCollection('pipelines');

:::tip[Sumbit your pipeline to nf-neuro !]
Have you built a pipeline using the [nf-neuro](https://github.com/nf-neuro) ecosystem ? Add it
to our listing of pipeline to improve its visibility. Open an [Issue here](https://github.com/nf-neuro/website/issues/new/choose)
and we'll guide you through the very simple process !
:::

<div class="not-content grid gap-6 mt-8">
  {pipelines.map((entry) => (
    <PipelineCard pipeline={entry.data} variant="detailed" />
  ))}
</div>

---
